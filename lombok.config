# Forces Lombok to use Jackson 2 for @Jacksonized.
# Needed when both Jackson 2 and Jackson 3 are present on the compile/annotation processor classpath.
config.stopBubbling = true
lombok.jackson.config = jackson2
