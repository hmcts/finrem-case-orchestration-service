java:
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    FEATURE_RESPONDENT_JOURNEY: true
  keyVaults:
    finrem:
      secrets:
        - finrem-case-orchestration-service-s2s-key
        - idam-secret
