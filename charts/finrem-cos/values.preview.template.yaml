java:
  # aadIdentityName needs to be unset as it's not used in preview env yet
  aadIdentityName:
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    IDAM_API_URL: "https://idam-api.aat.platform.hmcts.net"
    SWAGGER_ENABLED: true
