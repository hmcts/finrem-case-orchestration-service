server.port=9000
management.endpoints.web.exposure.include=*
management.endpoints.web.exposure.exclude=beans
management.endpoint.shutdown.enabled=false

spring.application.name=finrem-cos
documentation.swagger.enabled=true

#Service Auth provider url
auth.provider.service.client.port=4502
idam.s2s-auth.url=${AUTH_PROVIDER_SERVICE_CLIENT_BASEURL:http://localhost:4502}
idam.s2s-auth.secret=${AUTH_PROVIDER_SERVICE_CLIENT_KEY:AAAAAAAAAAAAAAAA}
idam.s2s.microservice=${AUTH_PROVIDER_SERVICE_CLIENT_MICROSERVICE:finrem_payment_service}


logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=DEBUG


# GENERAL SPRING BOOT ACTUATOR CONFIG
# Context path for Spring Boot Actuator endpoints
management.endpoints.web.base-path=/

management.info.defaults.enabled=true

# HEALTH ENDPOINT CONFIG
# Enable the health endpoint
management.endpoint.health.enabled=true
management.endpoint.health.show-details=always

# Enable sensitive health information
management.endpoints.health.sensitive=false

# Defines the URL of the health check to ${management.context-path}/health
management.endpoints.health.id=health

# Caching period in milliseconds of health endpoint
management.endpoint.health.cache.time-to-live=1000

# Enable default health indicators
management.health.defaults.enabled=true

# Enable database health check
management.health.db.enabled=true

# Enable disk space health check
management.health.diskspace.enabled=true

# METRICS ENDPOINT CONFIG
# Enable the metrics endpoint
management.endpoint.metrics.enabled=true

azure.app_insights_key=${APPINSIGHTS_INSTRUMENTATIONKEY:false}
spring.jackson.date-format=yyyy-MM-dd

# IDAM service
idam.url=${IDAM_API_URL:http://localhost:4501}
idam.api=/details
idam.health.url=${IDAM_API_URL:http://localhost:4501}/health

# FeeResponse service
fees.url=${FEES_API_URL:http://fees-register-api-aat.service.core-compute-aat.internal}
fees.health.url=${FEES_API_URL:http://fees-register-api-aat.service.core-compute-aat.internal}/health
fees.api=/fees-register/fees/lookup
fees.channel=default
fees.event=general application
fees.jurisdiction1=family
fees.jurisdiction2=family court
fees.keyword=without-notice
fees.service=other


# Payment service
payment.url=${PAYMENT_API_URL:http://payment-api-aat.service.core-compute-aat.internal}
payment.api=/credit-account-payments
payment.description=Financial Remedy Consented Application
payment.siteId=AA03

# PBA service
pba.validation.url=${PRD_API_URL:http://localhost:9000}
pba.validation.api=/search/pba/

#Finrem Notification service
finrem.notification.url=${FINREM_NOTIFICATION_SERVICE_BASE_URL:http://localhost:8086}
finrem.notification.health.url=${FINREM_NOTIFICATION_SERVICE_BASE_URL:http://localhost:8086}/health
finrem.notification.api=/notify
finrem.notification.hwfSuccessful=/hwf-successful
finrem.notification.assignToJudge=/assign-to-judge
finrem.notification.consentOrderMade=/consent-order-made
finrem.notification.consentOrderNotApproved=/consent-order-not-approved
finrem.notification.consentOrderAvailable=/consent-order-available

# Document
document.miniFormA.template=FL-FRM-APP-ENG-00002.docx
document.generator.service.api.baseurl=${DOCUMENT_GENERATOR_SERVICE_API_BASEURL:http://localhost:4009}
document.generator.service.api.health.url=${DOCUMENT_GENERATOR_SERVICE_API_BASEURL:http://localhost:4009}/health

http.timeout=30000
http.requestTimeout=30000
http.readTimeout=30000