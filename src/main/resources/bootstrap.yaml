spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/finrem
      aliases:
        AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        finrem-case-orchestration-service-s2s-key: idam.s2s-auth.totp_secret
        idam-secret: OAUTH2_CLIENT_FINREM
