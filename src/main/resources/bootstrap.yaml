spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: true
      paths: /mnt/secrets/finrem
      aliases:
        finrem.AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        finrem.finrem-case-orchestration-service-s2s-key: AUTH_PROVIDER_SERVICE_CLIENT_KEY
        finrem.idam-secret: OAUTH2_CLIENT_FINREM